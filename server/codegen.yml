overwrite: true
schema: "http://localhost:4000"
generates:
  ./generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./generated/graphql.schema.json:
    plugins:
      - "introspection"
